# Core
pandas>=1.5.0
numpy>=1.24.0

# Data
yfinance>=0.2.0

# Machine Learning
xgboost>=1.7.0
scikit-learn>=1.2.0

# Hyperparameter optimization (training only)
optuna>=3.0.0

# Broker APIs
# Install ib_insync separately (may require compilation):
#   pip install ib_insync
# ib_insync>=0.9.86

# Longbridge SDK for streaming (install separately):
#   pip install longbridge
# longbridge>=1.0.0

# YAML config parsing
PyYAML>=6.0

# Parquet support (built into pandas with pyarrow)
pyarrow>=10.0.0